Generate documentation for the Mini Library Management System code I provide.

Output two things:

1. JSDoc comments for every function:
- @param with type and description
- @returns with type and description
- @throws if the function can throw errors
- One-line description at the top

2. README section in markdown:
- Function name as heading
- What it does in 1-2 sentences
- Example usage with realistic library data (book titles, member names)